{
  "name": "jimmy-portfolio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "files": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"Get-ChildItem -Recurse -Force -Name | Where-Object { $_ -notmatch '(^|\\\\)(node_modules|\\.vscode|\\.astro|\\.git)(\\\\|$)' } | Out-File -FilePath listing.txt -Encoding utf8\""
  },
	"dependencies": {
		"@astrojs/sitemap": "^3.7.3",
		"@sentry/astro": "^10.70.0",
		"astro": "^7.2.1"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.4"
	}
}
